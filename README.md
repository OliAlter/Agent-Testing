# Task Manager API
A simple API to manage todos. 

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Run: `python src/main.py`

## TODO
- [ ] Add database persistence (currently uses in-memory dict)
- [ ] Fix the bug in the delete endpoint
- [ ] Add auth